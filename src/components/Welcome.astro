---
import Arrow from './icons/Arrow-icon.astro';
---
<section class="welcome">
    <div class="container">
        <div class="welcome__content">
            <div class="welcome__heading animate-on-scroll">
                <Arrow/> <span class="welcome__heading-text">Кто мы</span> <div class="welcome__heading-line"></div>
            </div>
            <div class="gradient-wrapper">
                <h1 class="welcome__title"><span>Добро пожаловать в</span> <span class="gradient-text">Oh&nbsp;Wait</span> — диджитал агентство, которое превращает <span class="gradient-text">идеи&nbsp;в&nbsp;результаты</span></h1>
            </div>
        </div>
        <spline-viewer class="spline-viewer" url="https://prod.spline.design/k-AwvNZgDFclKhVx/scene.splinecode"></spline-viewer>
    </div>
</section>

<style lang="scss">
    .welcome {
        position: relative;
        padding: 100px 0;
        background: #fff;
        height: 100vh;
        min-height: 650px;
        width: 100%;
        overflow: hidden;
    }
    .spline-viewer {
        position: absolute;
        top:0;
        left: 0;
        width: 100%;
        height: 100%;
    }
    .welcome__content {
        color: #000;
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 20px;
    }
    .welcome__heading {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 130px;

    }
    .welcome__heading-text {
        flex-shrink: 0;
        font-size: 16px;
        letter-spacing: 0.05em;
        font-weight: 300;
        text-transform: uppercase;
    }       
    .welcome__heading-line {
        width: 100%;
        height: 1px;
        background: #000;
    }
    .welcome__title {
        font-weight: 300;
        font-size: 48px;
        line-height: 60px;
        letter-spacing: 0.03em;
        text-transform: uppercase;
        color: #000000;
        text-indent: 35%;

        @media (max-width: 768px) {
            font-size: 36px;
            line-height: 1.2;
        }
    }

    .gradient-wrapper {
        background: linear-gradient(90deg, #FF0033 0%, #0A00DD 100%);
        -webkit-background-clip: text;
        background-clip: text;

    }

    .gradient-text {
        font-weight: 700;
        -webkit-text-fill-color: transparent;
    }
</style>